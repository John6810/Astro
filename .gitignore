dist/
node_modules/
.astro/
.env
.env.*
!.env.example
.DS_Store
*.log
.vscode/
.idea/
.claude/settings.local.json
# Local-only git worktrees used for parallel branches / Claude Code
# sessions. The actual feature branches live in the repo's main
# .git/ directory — these worktrees are pointers, not content.
.claude/worktrees/
# Wrangler dry-run output (build artifacts), regenerated on demand.
.wrangler/
# Istanbul coverage report — regenerated on every `pnpm test --coverage`.
coverage/
# Local-only asset dump (Azure architecture icons etc.) — not used
# at build time, not part of the dist output. Lives here for the
# repo owner's convenience.
/Icons/
